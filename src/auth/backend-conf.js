export const backendURL = process.env.REACT_APP_BACKEND_URL || 'http://localhost:3000';

export const routes = {
    register: "/register",
    login: "/login",
    fetchUser: "/config",
    config: "/config"
}